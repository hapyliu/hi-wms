<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haifeng.basedata.mapper.BaseMaterialCategoryMapper">

    <update id="updateCategoryChildren" parameterType="list">
        update base_material_category set ancestors =
        <foreach collection="list" item="item" index="index"
                 separator=" " open="case id" close="end">
            when #{item.id} then #{item.ancestors}
        </foreach>
        where id in
        <foreach collection="list" item="item" index="index"
                 separator="," open="(" close=")">
            #{item.id}
        </foreach>
    </update>

    <select id="selectChildrenCategoryById" resultType="com.haifeng.basedata.domain.BaseMaterialCategory">
        select * from base_material_category where del_flag = '0' and find_in_set(#{id}, ancestors)
    </select>

    <!-- 查询分类下的物料列表（包括子孙分类） -->
    <select id="selectMaterialsByCategoryId" resultType="com.haifeng.basedata.domain.BaseMaterial">
        select m.* 
        from base_material m
        inner join (
            select id 
            from base_material_category 
            where find_in_set(#{categoryId}, ancestors) or id = #{categoryId}
        ) mc on find_in_set(m.category_id, mc.id)
        where m.del_flag = '0'
    </select>

</mapper>
